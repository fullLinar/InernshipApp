import React from 'react';
import { Text, View } from 'react-native';

const PrayerInfo = (props) => {
  return (
    <View>
      <Text>Its prayer</Text>
    </View>
  );
};

export default PrayerInfo;
